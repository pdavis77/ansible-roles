---
#
# File: nginx/tasks/main.yml
#

# Nginx config
- include: "nginx-{{ os_family }}.yml"


- name: Upload nginx.conf
  template: src=nginx.conf.j2 dest=/etc/nginx/nginx.conf
  notify:
    - restart nginx
